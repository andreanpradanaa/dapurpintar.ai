-- +goose Up
-- M5-001 base schema placeholder. Full schema DDL is applied as part of M7
-- database adapter implementation and reviewed against docs/database/m5-schema.md.
SELECT 1;

-- +goose Down
SELECT 1;
